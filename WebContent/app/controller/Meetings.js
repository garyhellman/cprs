Ext.define('CPRS.controller.Meetings', {
    extend: 'Ext.app.Controller',

    init: function() {
        console.log('Initialized Meetings! This happens before the Application launch function is called');
    }
});