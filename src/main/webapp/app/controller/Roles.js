Ext.define('CPRS.controller.Roles', {
    extend: 'Ext.app.Controller',

    init: function() {
        console.log('Initialized Roles! This happens before the Application launch function is called');
    }
});